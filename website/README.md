# Website (Vite + React + Tailwind)

Quick start:

```bash
cd website
npm install
npm run dev
```

Notes:
- This project uses Vite, React, React Router, and Tailwind.
- Edit files under `src/` — layouts are in `src/layouts`, pages in `src/pages`.
- Tailwind config is in `tailwind.config.cjs`.
